# Supabase
SUPABASE_URL=
SUPABASE_KEY=

# LLM provider (used by agent/langchain_agent.py)
ANTHROPIC_API_KEY=

# Google Places (store lookup, used by agent/tools.py check_store_stock)
GOOGLE_PLACES_API_KEY=

# Stripe test mode (used by agent/tools.py place_order)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Used by capture/webcam_capture.py — set to http://localhost:8010 to match
# the port docker-compose.yml maps the backend to on this machine
BACKEND_URL=http://localhost:8000
